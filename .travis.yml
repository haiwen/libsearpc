language: c
sudo: false
addons:
  # See http://docs.travis-ci.com/user/apt/
  apt:
    packages:
      - libjansson-dev
compiler:
 - gcc
 - clang
before_install:
 - git clean -x -f
 - ./autogen.sh
script:
 - ./configure
 - make -j8
 - make check
notifications:
    email: false
