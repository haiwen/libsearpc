sudo: false
language: c
sudo: false
addons:
  # See http://docs.travis-ci.com/user/apt/
  apt:
    packages:
      - libjansson-dev
compiler:
 - gcc
 - clang
addons:
  apt:
    packages:
      - libjansson-dev
before_install:
 - git clean -x -f
 - ./autogen.sh
script:
 - ./configure
 - make -j8
 - make check -j8
notifications:
    email: false
